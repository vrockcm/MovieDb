<% include partials/header.ejs %>
<div class="container">
    <% if (message) { %>
        <p class="text-center text-danger"><%= message %></p>
    <% } %>

    <% if (actor) { %>
        <form class="add-player-form" action="" method="post" enctype="multipart/form-data">
		    <div class="form-row">
				Modify an existing actor's record, input full name, birth date, and award(s). Note that name and birth date are required fields. 
			</div>
            <div class="form-row">
                <div class="form-group col-md-4">
                    <label for="name">Full Name</label>
                    <input type="text" class="form-control" name="name" id="name" value="<%= actor.name %>" required>
                </div>
                <div class="form-group col-md-4">
                    <label for="birth">Birth Date</label>
                    <input type="date" class="form-control" name="birth" id="birth" value="<%= actor.birth.toISOString().substring(0, 10) %>" required>
                </div>
				<div class="form-group col-md-4">
                    <label for="award">Award(s)</label>
                    <input type="text" class="award" name="award" id="award" value="<%= actor.award %>">
                </div>
            </div>
            <button type="submit" class="btn btn-success float-right">Update Actor</button>
        </form>
    <% } else { %>
        <p class="text-center">Actor Not Found. Go <a href="/add">here</a> to add actors.</p>
    <% } %>
</div>
</div>
</body>
</html>