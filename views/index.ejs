<% include partials/header.ejs %>
    <div class="table-wrapper">
        <div class="container-fluid">
            <div class="row">
                <div class="col-12 col-xl-9">
                    <h3 align="center">Movie</h3>
                    <% if (movie.length > 0) {%>
                        <table class="table table-hovered table-bordered table-striped">
                            <thead class="thead-dark">
                                <tr>
                                    <th scope="col">ID</th>
                                    <th scope="col">Name</th>
                                    <th scope="col">Rating</th>
                                    <th scope="col">Release Date</th>
                                    <th scope="col">Duration</th>
                                    <th scope="col">Genre</th>
                                    <th scope="col">Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% movie.forEach((movie, index) => { %>
                                    <tr>
                                        <th scope="row"><%= movie.id %></th>
                                        <td><%= movie.name %></td>
                                        <td><%= movie.rating %></td>
                                        <td><%= movie.released.toISOString().substring(0, 10)  %></td>
                                        <td><%= movie.duration %></td>
                                        <td><%= movie.genre %></td>
                                        <td>
                                            <a href="/editMovie/<%= movie.id %>" target="_blank" rel="noopener" class="btn btn-sm btn-success">Edit</a>
                                            <a href="/deleteMovie/<%= movie.id %>" class="btn btn-sm btn-danger">Delete</a>
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    <% } else { %>
                        <p class="text-center">No movies found. Go <a href="/addMovie" >here</a> to add movies.</p>
                    <% } %>
                </div>
                <div class="col-12 col-xl-3">
                    <h3 align="center">Distributor</h3>
                    <% if (distributor.length > 0) {%>
                        <table class="table table-hovered table-bordered table-striped">
                            <thead class="thead-dark">
                                <tr>
                                    <th scope="col">ID</th>
                                    <th scope="col">Name</th>
                                    <th scope="col">Location</th>
                                    <th scope="col">Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% distributor.forEach((distributor, index) => { %>
                                    <tr>
                                        <th scope="row"><%= distributor.id %></th>
                                        <td><%= distributor.name %></td>
                                        <td><%= distributor.location %></td>
                                        <td>
                                            <a href="/editDistributor/<%= distributor.id %>" target="_blank" rel="noopener" class="btn btn-sm btn-success">Edit</a>
                                            <a href="/deleteDistributor/<%= distributor.id %>" class="btn btn-sm btn-danger">Delete</a>
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    <% } else { %>
                        <p class="text-center">No distributors found. Go <a href="/addDistributor" >here</a> to add movies.</p>
                    <% } %>
                </div>

            </div>
            <div class="row">
                <div class="col-12 col-xl-4">
                    <h3 align="center">Actor</h3>
                    <% if (actor.length > 0) {%>
                        <table class="table table-hovered table-bordered table-striped">
                            <thead class="thead-dark">
                                <tr>
                                    <th scope="col">ID</th>
                                    <th scope="col">Name</th>
                                    <th scope="col">Award</th>
                                    <th scope="col">BirthDate</th>
                                    <th scope="col">Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% actor.forEach((actor, index) => { %>
                                    <tr>
                                        <th scope="row"><%= actor.id %></th>
                                        <td><%= actor.name %></td>
                                        <td><%= actor.award %></td>
                                        <td><%= actor.birth.toISOString().substring(0, 10)  %></td>
                                        <td>
                                            <a href="/editActor/<%= actor.id %>" target="_blank" rel="noopener" class="btn btn-sm btn-success">Edit</a>
                                            <a href="/deleteActor/<%= actor.id %>" class="btn btn-sm btn-danger">Delete</a>
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    <% } else { %>
                        <p class="text-center">No actors found. Go <a href="/addActor" >here</a> to add actors.</p>
                    <% } %>

                </div>
                <div class="col-12 col-xl-4">
                    <h3 align="center">Director</h3>
                    <% if (director.length > 0) {%>
                        <table class="table table-hovered table-bordered table-striped">
                            <thead class="thead-dark">
                                <tr>
                                    <th scope="col">ID</th>
                                    <th scope="col">Name</th>
                                    <th scope="col">Award</th>
                                    <th scope="col">BirthDate</th>
                                    <th scope="col">Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% director.forEach((director, index) => { %>
                                    <tr>
                                        <th scope="row"><%= director.id %></th>
                                        <td><%= director.name %></td>
                                        <td><%= director.award %></td>
                                        <td><%= director.birth.toISOString().substring(0, 10)  %></td>
                                        <td>
                                            <a href="/editDirector/<%= director.id %>" target="_blank" rel="noopener" class="btn btn-sm btn-success">Edit</a>
                                            <a href="/deleteDirector/<%= director.id %>" class="btn btn-sm btn-danger">Delete</a>
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    <% } else { %>
                        <p class="text-center">No directors found. Go <a href="/addDirector" >here</a> to add directors.</p>
                    <% } %>

                </div>
                <div class="col-12 col-xl-4">
                     <h3 align="center">Producer</h3>
                    <% if (producer.length > 0) {%>
                        <table class="table table-hovered table-bordered table-striped">
                            <thead class="thead-dark">
                                <tr>
                                    <th scope="col">ID</th>
                                    <th scope="col">Name</th>
                                    <th scope="col">Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% producer.forEach((producer, index) => { %>
                                    <tr>
                                        <th scope="row"><%= producer.id %></th>
                                        <td><%= producer.name %></td>
                                        <td>
                                            <a href="/editProducer/<%= producer.id %>" target="_blank" rel="noopener" class="btn btn-sm btn-success">Edit</a>
                                            <a href="/deleteProducer/<%= producer.id %>" class="btn btn-sm btn-danger">Delete</a>
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    <% } else { %>
                        <p class="text-center">No producers found. Go <a href="/addProducer" >here</a> to add producers.</p>
                    <% } %>

                </div>
            </div>
        </div>
    </div>
</body>
</html>